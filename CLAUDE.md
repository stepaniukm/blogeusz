# Project Instructions

## Code Style

### No Comments
Do not add any comments to the codebase — no single-line (`//`), multi-line (`/* */`), HTML (`<!-- -->`), or CSS comments.
Code must be self-explanatory through clear naming and structure.
The only exception is if a comment is absolutely necessary AND the user has explicitly granted permission for that specific case.
