---
import { getLangFromUrl, useTranslations } from '../i18n/utils';
import { getRelativeLocaleUrl } from 'astro:i18n';

interface Tab {
  label: string;
  href: string;
  key: string;
}

interface Props {
  active: string;
}

const { active } = Astro.props;

const locale = getLangFromUrl(Astro.url);
const t = useTranslations(locale);

const tabs: Tab[] = [
  { label: t('category.dev'), href: getRelativeLocaleUrl(locale, 'blog'), key: 'dev' },
  { label: t('category.photos'), href: getRelativeLocaleUrl(locale, 'photos'), key: 'fotografia' },
  { label: t('category.til'), href: getRelativeLocaleUrl(locale, 'til'), key: 'til' },
];
---

<div class="tabs" role="tablist">
  {
    tabs.map((tab) => (
      <a
        href={tab.href}
        class:list={['tab', tab.key === active && 'tab-active']}
        role="tab"
        aria-selected={tab.key === active}
      >
        {tab.label}
      </a>
    ))
  }
</div>

<style>
  .tabs {
    display: flex;
    gap: 0;
    border-radius: var(--radius-tag);
    overflow: hidden;
    border: 1px solid var(--color-border);
    width: fit-content;
  }

  .tab {
    padding: 0.4375rem 1.25rem;
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-text-muted);
    text-decoration: none;
    background-color: var(--color-bg-card);
    border-right: 1px solid var(--color-border);
    transition:
      background-color 0.15s,
      color 0.15s;
  }

  .tab:last-child {
    border-right: none;
  }

  .tab:hover {
    color: var(--color-text);
    background-color: var(--color-tag-bg);
  }

  .tab-active {
    background-color: var(--color-accent);
    color: var(--color-bg-dark, #1a1f2e);
    font-weight: 700;
  }

  .tab-active:hover {
    background-color: var(--color-accent-hover);
    color: var(--color-bg-dark, #1a1f2e);
  }
</style>
